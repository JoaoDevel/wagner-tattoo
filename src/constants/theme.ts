import { colors } from "./colors";
import { fonts } from "./fonts";
import { spacing } from "./spacing";

/** Tokens de diseño (referencia). Las clases visuales están en `classes.ts`. */
export const theme = {
  colors,
  fonts,
  spacing,
} as const;
